{
  "mcpServers": {
    "clawcode": {
      "command": "bash",
      "args": [
        "-c",
        "npm install --prefix \"${CLAUDE_PLUGIN_ROOT}\" --silent >/dev/null 2>&1; exec \"${CLAUDE_PLUGIN_ROOT}/node_modules/.bin/tsx\" \"${CLAUDE_PLUGIN_ROOT}/server.ts\""
      ]
    }
  }
}
